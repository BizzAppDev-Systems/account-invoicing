- To enable automatic partner detection based on geolocation during OCR invoice processing, configure the Maps API key by setting the geocode.autocomplete_url system parameter.

- If no matching partner is found using name, VAT, or geocode data, the system automatically creates a new partner record.